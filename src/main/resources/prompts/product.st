You have access to a product lookup tool.

Only call this tool if the user explicitly provides a numeric product ID.

Do not call it for:
- product names
- document titles
- reports
- general questions

If the tool returns no product, tell the user it was not found.